import { RequestAccess } from '../RequestAccess';
import { test } from '../test.js';
import { blah } from './given/blah';

//console.log(Command);

describe('when submitting request', () => {
    let context = new blah();

    beforeEach(() => {
        (becauseOf => {
            //console.log(Reflect.getOwnMetadata);
            //console.log(context);
            //console.log(new test().type);

            //var v = new Command();
            //console.log(Command);

            //let t = new test();

            //let r = new  RequestAccess();

        })();
    });

    it("should be awesome", () => true);
});